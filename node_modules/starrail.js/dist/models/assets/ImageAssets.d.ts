import { StarRail } from "../../client/StarRail";
export interface ImageBaseUrl {
    filePath: "UPPER_CAMEL_CASE" | "LOWER_CASE" | "NONE";
    url: string;
    regexList: RegExp[];
    priority: number;
    format: "PNG" | "WEBP" | "JPG";
}
export interface CustomImageBaseUrl extends ImageBaseUrl {
    /**
     * @param path path formatted by [ImageBaseUrl#filePath](ImageBaseUrl#filePath)
     * @returns parsed path
     */
    customParser: (path: string) => string;
}
export declare class ImageAssets {
    readonly client: StarRail;
    readonly path: string;
    readonly imageBaseUrl: ImageBaseUrl | null;
    readonly url: string;
    readonly isAvailable: boolean;
    constructor(path: string, client: StarRail, maxPriority?: number);
    /**
     * @returns a new instance of ImageAssets with the another imageBaseUrl
     */
    nextSource(): ImageAssets | null;
}
