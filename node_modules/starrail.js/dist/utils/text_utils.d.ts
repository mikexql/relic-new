export declare class TextUtils {
    static readonly rubyRegex: RegExp;
    static removeRuby(text: string): string;
    static replaceRuby(text: string, replacer: (match: string, base: string, ruby: string) => string): string;
}
