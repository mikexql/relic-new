"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nonNullable = void 0;
exports.getValuesFromSimpleMap = getValuesFromSimpleMap;
exports.getKeysFromSimpleMap = getKeysFromSimpleMap;
const nonNullable = (value) => value !== null;
exports.nonNullable = nonNullable;
function getValuesFromSimpleMap(map) {
    const values = [];
    map.forEach((value) => {
        if (value instanceof Map) {
            values.push(...getValuesFromSimpleMap(value));
        }
        else {
            values.push(value);
        }
    });
    return values;
}
function getKeysFromSimpleMap(map) {
    const keys = [];
    map.forEach((_, key) => {
        keys.push(key);
        if (map.get(key) instanceof Map) {
            keys.push(...getKeysFromSimpleMap(map.get(key)));
        }
    });
    return keys;
}
